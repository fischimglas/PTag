# 13 – README

**Problems** 'Empty container' result shows a link that is never added; setStyle line says 'attributes';
no note that child content is not escaped; class merge (05) undocumented.

**Plan** Fix example, wording, add 'Escaping' section, document new constant and boolean attribute handling.
